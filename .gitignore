.DS_Store
.idea
npm-debug.log
/components
/node_modules

# The following are ignore using --assume-unchanged
# To view a list of files marked "unchanged": `git ls-files -v`
# To recursively update: `git ls-files -z myFolderToIgnore/ | xargs -0 git update-index --assume-unchanged`
# Or: `git ls-files -z myFolderToIgnore/ | xargs -0 git update-index --no-assume-unchanged`
#
# dist/